services:
  - type: web
    name: loan-prediction-project
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn loan_prediction_project.wsgi:application"
    envVars:
      - key: DEBUG
        value: False